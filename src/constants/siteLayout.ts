/**
 * Shared page width + horizontal padding (aligned with hero and main sections).
 * Matches reference-style centred content column (e.g. BairesDev-style max width).
 */
export const SITE_CONTAINER_CLASS =
  'mx-auto w-full max-w-screen-2xl px-4 sm:px-6 lg:px-8';
